=== grep exercice ===

grep -n 'CU_UNREFERENCED_PARAMETER(pTest)' ./Sources/Console/Console.c
grep -r 'assert' ./*
grep -r 'assert' ./* | wc -l

=== pipe exercice ===

grep '#' input.txt | sort -d | uniq > output.txt

=== tar ===

tar -cvzf output.tar.gz file folder1 folder2

=== diff ===

diff -sg syslog.log student1.log
diff -sg syslog.log student2.log
diff -sg syslog.log student3.log